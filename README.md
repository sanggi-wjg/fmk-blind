# FMK-Blind

에펨코리아(PC, `www.fmkorea.com`) 특정 유저를 블라인드 처리하는 크롬 확장(Manifest V3).
차단한 작성자의 글·댓글을 완전히 숨긴다.

## 설치 (압축해제 로드)

1. `chrome://extensions` 접속
2. 우측 상단 **개발자 모드** 켜기
3. **압축해제된 확장 프로그램 로드** → 이 저장소 루트 폴더 선택

## 사용법

- **차단/해제**: fmkorea에서 작성자 닉네임을 **우클릭** → 커스텀 메뉴에서 차단 또는 해제
- **목록 관리**: 툴바의 확장 아이콘 클릭 → 팝업에서 차단 목록·인원수 확인, 검색, 차단 해제
- 차단/해제는 **열려 있는 다른 fmkorea 탭에도 새로고침 없이 즉시 반영**된다(`chrome.storage.onChanged`).

## 권한·저장

- 권한은 **`storage`** 하나뿐 — 외부로 데이터를 전송하지 않는다.
- 차단 목록은 `chrome.storage.sync`에 저장되어 로그인된 크롬 기기 간 동기화된다.

## 참고·한계

- **PC 전용**(`www.fmkorea.com`). 모바일(`m.fmkorea.com`)은 미지원.
- 페이지 로드 시점에 1회 스캔해 숨긴다. **AJAX·무한스크롤로 새로 불러온 글**은 새로고침 시 반영된다(증분 처리 `MutationObserver`는 후속 과제 — `TODO.md` 참고).
- **여러 컴퓨터에서 사용**: 확장 자체(코드)는 기기마다 따로 로드해야 한다(압축해제 확장은 코드가 동기화되지 않음). 단 manifest의 고정 `key`로 확장 ID가 어느 기기에서나 동일하므로, **차단 목록은 같은 구글 계정 + 크롬 동기화면 자동으로 기기 간 공유**된다(`chrome.storage.sync`).

## 라이선스

[MIT](LICENSE) © 2026 sanggi-wjg
